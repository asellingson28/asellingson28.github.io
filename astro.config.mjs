import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://aselling.io',
  // No `base` needed if deploying to a custom domain or username.github.io root.
  // If deploying to username.github.io/repo-name instead, uncomment:
  // base: '/repo-name',
});
