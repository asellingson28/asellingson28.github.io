{
  "name": "aselling-blog-subscribe-worker",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "subscribers": "wrangler kv key get confirmed --binding SUBSCRIBERS --remote"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
